import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Auth = () => {
  return (
    <View>
      <Text />
    </View>
  );
};

export default Auth;

const styles = StyleSheet.create({});
